

export default function OneAccountFeatures({icon,title,description}){
return(
    <div className="OneAccountFeatures">
        <span>{icon}</span>
        <p>{title}</p>
        <p>{description}</p>
    </div>
)
}